class ColumnMeta {
  ColumnMeta({required this.width,required this.fixedWidth, required this.isFreezed, required this.isSelected, required this.isHover, required this.sortEnabled});
  double width;
  double fixedWidth;
  bool isFreezed;
  bool sortEnabled;
  bool isHover;
  bool isSelected;
}